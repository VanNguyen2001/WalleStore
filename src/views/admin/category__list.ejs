<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Danh Mục Sản Phẩm</title>
  <link rel="stylesheet" href="/css/style.css">
  </link>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
</head>

<body>
  <div class="container">
    <%- include("menu.ejs")%>
      <h2 class="text-center p-5">Danh Mục Sản Phẩm</h2>
      <table class="table">
        <thead>
          <tr>
            <th scope="col">Tên Danh Mục</th>
            <th scope="col">Mổ Tả</th>
            <th scope="col">Hình Minh Hoạ</th>
            <th class="text-center" scope="col">Tuỳ chọn</th>
          </tr>
        </thead>
        <tbody>
          <% for(let i=0; i < dataCategory.length; i++) { %>
            <tr>
              <td>
                <%= dataCategory[i].name %>
              </td>
              <td>
                <%= dataCategory[i].describe %>
              </td>
              <td><img src="/img/<%= dataCategory[i].image %>" width="50px" height="50px" style="border-radius: 5px;">
              </td>
              <td>
                <div class="row">
                  <form class="col-md-6" action="/edit-category/<%= dataCategory[i].id %>" method="GET">
                    <button type="submit" class="btn btn-dark">Xem</button>
                  </form>
                  <form class="col-md-6" action="/delete-category" method="post">
                    <input type="text" hidden value="<%= dataCategory[i].id %>" name="categoryId" />
                    <button type="submit" class="btn btn-dark">Xoá</button>
                  </form>
                </div>
              </td>
            </tr>
            <% } %>
        </tbody>
      </table>
      <div class="row">
        <form action="/create-new-category" method="POST" class="col-md-6 offset-md-3 pt-5">
          <div class="form-group">
            <label>Tên Danh Mục</label>
            <input type="text" class="form-control" placeholder="Nhập tên của hãng" name="name">
          </div>
          <div class="form-group">
            <label>Mô Tả</label>
            <input type="text" class="form-control" placeholder="Thêm mô tả" name="describe">
          </div>
          <div class="form-group">
            <label>Hình Minh Hoạ</label>
            <input type="file" class="form-control" placeholder="Chọn hình minh hoạ" name="image">
          </div>
          <br><button type="submit" class="btn btn-primary">Thêm</button></br>
        </form>
      </div>
  </div>
</body>

</html>